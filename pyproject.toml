[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ris-API-wrapper"
version = "0.1.0"
description = "A wrapper for the Austrian RIS API"
dependencies = [
    "requests",
]

[tool.setuptools]
packages = ["risApiWrapper"]
